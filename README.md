# -Neural-Network-for-Regression-
Implemented a Fully Connected Neural Network (FCNN) for regression using PyTorch. The project includes dataset preprocessing, train-validation-test splitting, model training with SGD optimizer, learning rate scheduling, TensorBoard visualization, model evaluation using MSE, and prediction analysis through scatter plots.
